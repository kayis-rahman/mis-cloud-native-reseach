# OS-specific files
.DS_Store
Thumbs.db

# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
# IntelliJ build output
out/

# VS Code (optional)
.vscode/

# Eclipse (optional)
.project
.classpath
.settings/

# Maven
# Keep wrapper files tracked; ignore general build outputs
# Do not ignore .mvn to allow wrapper; many teams commit wrapper
# If you decide to ignore, remove the next two negations
!/.mvn/wrapper/maven-wrapper.jar
!/.mvn/wrapper/maven-wrapper.properties
# Build outputs
target/

# Gradle (if present in this repo)
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Logs and temporary files
logs/
*.log
*.tmp
*.swp
*.swo

# Spring Boot specific local config overrides
application-local.properties
application-local.yml
application-*.local.properties
application-*.local.yml

# Environment files (secrets)
.env
.env.*

# Docker local overrides
docker-compose.override.yml

# Node (if this service adds a UI later)
node_modules/
coverage/

# MacOS metadata
.AppleDouble
.LSOverride

# Windows metadata
Desktop.ini
$RECYCLE.BIN/
